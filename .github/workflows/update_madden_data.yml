name: Update Madden Data
'on':
  workflow_dispatch: {}
  schedule:
  - cron: 0 10 * * 1
permissions:
  contents: write
jobs:
  update-madden-data:
    runs-on: ubuntu-latest
    steps:
    - name: Check out repository
      uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    - name: Install dependencies
      run: 'python -m pip install --upgrade pip

        pip install -r requirements.txt'
    - name: Update Madden ratings
      run: python update_madden_data.py
    - name: Show generated files
      run: ls -lh data/madden_27_*
    - name: Commit updated Madden data
      run: "git config user.name \"github-actions[bot]\"\ngit config user.email \"\
        41898282+github-actions[bot]@users.noreply.github.com\"\ngit add data/madden_27_players.csv\
        \ data/madden_27_raw.json data/madden_27_metadata.json data/madden_27_team_ratings.json\n\
        if git diff --cached --quiet; then\n  echo \"No Madden data changes to commit.\"\
        \nelse\n  git commit -m \"Update Madden 27 ratings data\"\n  git push\nfi"
