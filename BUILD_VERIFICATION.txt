Macabets v0.23 — NFL Data Pipeline
Build date: July 23, 2026

Verified locally:
- app.py and every engine module compile successfully.
- Existing tennis files and ATP CSV data remain present.
- NFL engine imports and produces a fair-line report in fallback mode.
- Synthetic play-by-play data successfully produces offense, defense and quarterback ratings.
- NFL refresh dependency is isolated; the app remains usable if nflverse is unavailable.
- GitHub Actions workflow syntax and repository paths were reviewed.

Network limitation during packaging:
- The packaging environment could not contact nflverse, so the first live snapshot must be generated by the included GitHub Action after upload.
